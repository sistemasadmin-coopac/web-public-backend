spring:
  datasource:
    url: jdbc:postgresql:///${DB_NAME:web-public-coopac-db}?cloudSqlInstance=${CLOUD_SQL_INSTANCE:coopac-elsalvador-backend:us-central1:coopac-elsalvador-web-public-db}&socketFactory=com.google.cloud.sql.postgres.SocketFactory&enableIamAuth=false
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      connection-timeout: 30000
      initialization-fail-timeout: -1
      leak-detection-threshold: 60000

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: America/Lima
    open-in-view: false

server:
  port: ${PORT:8080}
  shutdown: graceful

cors:
  allowed:
    origins:
      - https://coopac-el-salvador-fronted.web.app
      - https://coopac-el-salvador-fronted.firebaseapp.com
      - https://coopacelsalvador.com
      - https://www.coopacelsalvador.com

file:
  storage:
    type: gcs
    gcs:
      bucket-name: ${GCS_BUCKET_NAME}
      project-id: ${GCS_PROJECT_ID:coopac-elsalvador-backend}
      base-url: https://storage.googleapis.com

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true

logging:
  level:
    root: INFO
    com.elsalvador.coopac: INFO
    com.google.cloud.sql: DEBUG
